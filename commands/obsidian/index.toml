description = "Index the vault for Semantic Search (Incremental by default)"
prompt = "Please run the `obsidian_rag_index` tool to index the vault for semantic search. This tool will perform incremental indexing by default, but you can use the `force_reindex` parameter to rebuild the index from scratch if needed."